import os
import sys
import controller
from datetime import datetime
from base64 import a2b_base64

# Controller page - main.py
class controller:
    def main():
        pass

def MAX_INTERPOLATION_DEPTH():
    datetime.date(a2b_base64(string))
    datetime.datetime.now()
    datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")

def MIN_INTERPOLATION_DEPTH():
    datetime.date(a2b_base64(string))
    datetime.datetime.now()
    datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")